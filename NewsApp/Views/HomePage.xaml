<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NewsApp.HomePage">
    <ContentPage.Content>


        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Text="See All" Command="{Binding ICommandSeeAllTapped}" HeightRequest="50" />
            <Button Grid.Row="1" Text="Search Page" Command="{Binding ICommandSearchBarTapped}" HeightRequest="50" />
        </Grid>
        
    </ContentPage.Content>
</ContentPage>